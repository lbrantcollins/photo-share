<!DOCTYPE html>
<html>
<%- include("../header") -%>
<body>

   <section id="gallery">
      <div class="container">
         <h1>Photo Details</h1>
         <div class="row">
            <div class="col-lg-6 mb-6">
               <div class="card">
                  <img class="card-img-top" src="<%=photo.url%>" alt="a photo">
                  <div class="card-body">
                     <h5 class="card-title"><%=photo.title%></h5>
                     <p class="card-text">by <a href="/users/<%=user._id%>" ><%=user.user%></a></p>
                     <p class="card-text"><%=photo.description%></p>
                        <div class=buttons>
                        	<form class=submit-button>
                        	  <button class="btn btn-secondary btn-sm" formaction="/photos/<%=photo._id%>?_method=DELETE" formmethod="POST" type="submit">Delete Photo</button>
                           </form>

                           <a href="/photos/<%=photo._id%>/edit" class="btn btn-secondary btn-sm">Edit Photo</a>
                         
                         	<a href="/photos" class="btn btn-outline-secondary btn-sm">Return to Photos List</a>
                     	
                        </div>
                  </div>
               </div>
            </div>
         </div>
      </div>
    </section>

</body>
</html>